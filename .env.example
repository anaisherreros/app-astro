# Copia este archivo a .env y rellena los valores ( .env no se sube a git ).
ANTHROPIC_API_KEY=sk-ant-api03-...

# En Railway (HTTPS): origen publico del sitio, evita 403 CSRF en POST /interpretation/
# CSRF_TRUSTED_ORIGINS=https://tu-servicio.up.railway.app
